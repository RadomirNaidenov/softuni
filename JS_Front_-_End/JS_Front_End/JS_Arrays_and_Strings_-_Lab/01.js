function sumNums(array) {
    // let firstNum = array.shift();
    // let lastNum = array.pop();

    // let firstNum = array.at(0);
    // let lastNum = array.at(-1);

    let firstNum = array[0];
    let lastNum = array[array.length - 1];

    console.log(firstNum + lastNum);
    
}
