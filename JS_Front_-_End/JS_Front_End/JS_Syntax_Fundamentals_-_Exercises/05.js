function printNums(baseNum) {
    let counter = 1;

    for (let num = 1; num <= 10; num++) {
        console.log(`${baseNum} X ${counter} = ${baseNum * counter}`);
        counter += 1
        
    }
}
